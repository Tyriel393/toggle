export function KitPage() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-16">
      <h1 className="text-2xl font-semibold">Component kit</h1>
      <p className="mt-3 text-ink-secondary">
        Empty until the design system is extracted from Toggl 2.0. Components land here with every
        state rendered side by side for fidelity checks against the real app.
      </p>
    </main>
  )
}
